*.pyc
.project
.pydevproject
.settings
*~

# IDE
.idea

# Rust
/target
**/*.rs.bk

# Node
node_modules
web/dist

# Local env
.env
.env.local

# Prod/dev data snapshots pulled by bin/pull-data. E-mails are anonymised, but
# real names remain — these stay on local disk only, never committed.
/snapshots/

# E2E run artefacts (per-run files are namespaced by the dynamic API port)
web/.e2e-table*
web/.e2e-api.*.pid
web/.e2e-api.*.log

# OpenTofu
infrastructure/.terraform/
infrastructure/.terraform.lock.hcl
infrastructure/env.auto.tfvars
*.tfstate
*.tfstate.*
*.tfplan

# Claude Code / agent framework — per-machine local state.
# Note: .claude/settings.json (no .local) IS shared and stays tracked.
.claude/settings.local.json
.claude/skills/
.claude/worktrees/
.agents/
skills-lock.json
